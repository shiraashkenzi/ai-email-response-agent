# =============================================================================
# Email Response Agent - Environment Variables
# =============================================================================
# 1. Copy this file to .env:    cp .env.example .env
# 2. Replace placeholder values below. Never commit .env to version control.
# =============================================================================

# OpenAI (required)
OPENAI_API_KEY=your_openai_api_key_here
# Model: gpt-4o-mini (default), or gpt-4o / gpt-4 if your account has access
OPENAI_MODEL=gpt-4o-mini

# Gmail OAuth (paths relative to project root; optional - defaults shown)
GMAIL_TOKEN_PATH=token.json
GMAIL_CREDENTIALS_PATH=credentials.json
